# TraceDigest：雾蓝灰粉界面与聊天陪伴人偶重做提案

状态：**Draft / 方案评审，不是已经完成的产品界面改造。**

## 核对基线

本提案针对 `Bluuok/TraceDigest` 的 `main` 提交 [`046564cea422d7edeee686f5c3b404090d15e6f6`](https://github.com/Bluuok/TraceDigest/commit/046564cea422d7edeee686f5c3b404090d15e6f6)，标题 `feat: add code-based companion doll`，提交时间 **2026-09-20 22:55:36（UTC+8）**。检查时 PR 列表为空；用户所说的“23:00 左右 PR”实际对应这次直接提交。

该提交相对父提交只增加了 `AppShell.tsx` 中的人偶及 `foundation.scss` 中的人偶样式，没有完成参考图的主题、首页布局和角色资产还原。后续实施应重新核对 main；不要基于更早的上游 gmll-star 仓库覆盖这里的功能。

## 结论

**页面按用户给定的暖白、雾蓝、灰粉插画风格还原；首页右上角保留大幅角色插画；进入实际聊天详情后，改为左下角的同角色 Q 版小人偶。首选精修分层原画 + Live2D，而不是继续把粗略 SVG 当作最终人物。**

本 PR 分为两个相互配合的实施方向：

| 文档 | 负责什么 |
| --- | --- |
| [01-ui-restoration.md](01-ui-restoration.md) | 主题为何灰暗、真实首页与聊天详情的分离、参考图布局、颜色与数据约束、代码落点 |
| [02-companion-rebuild.md](02-companion-rebuild.md) | 人偶缺陷、2.5D / 真 3D 选型、角色资产交付、跟随与遮罩、出场位置、验收 |
| [current-companion-repro.html](current-companion-repro.html) | 当前提交的 SVG 与跟随算法隔离复现；不是新版效果图 |
| [validation.json](validation.json) | 本次隔离复现与建议配色计算的实际检查结果 |

参考图是用户本轮上传的 TraceDigest「C · 雾蓝灰粉」1073 × 668 图片。本 PR 未复制图片文件；实施与视觉评审时应使用这张原图，而不是仅凭文字另起一种设计。画面关键特征已写入第一份文档。

## 已验证与未验证

已读取绑定到上述 SHA 的主题、入口、AppShell、导航、AskAIWorkspace 与 TopicPanel 的相关实现，并对这次提交和父提交做了文件级比较。额外在 Chromium 144 中渲染了隔离复现：7 个人偶实例正常加载、无页面脚本错误；鼠标相对原尺寸人偶中心横向 +100 px、纵向 -100 px 时，原始算法得到 `(1, -1)`，已经饱和。三个固定方向分别展示正视、右上、右下极限；放大只用于观察，不改变人物几何。

没有启动完整 Electron 应用，没有连接用户的微信数据库，没有运行仓库全套构建/测试，也没有制作或测试新的 Live2D 模型。因此不能用本 PR 的检查结果宣称新版 UI 已还原、人偶已经变可爱，或 Windows 打包已经通过。

## 交付顺序与停止条件

1. **界面实施 PR**：先完成暖白雾蓝灰粉主题、独立话题首页与聊天详情导航，保留原有日报、Clawbot、导出和设置；提交同尺寸真实页面截图。
2. **角色实施 PR**：先交付静态角色审美稿，再交付带眼睛遮罩和头部变形的独立动效样例，最后接入聊天详情。美术稿不过关，不继续写更多跟随动画掩盖问题。

两项工作可以并行，但角色运行时接入依赖明确的聊天详情状态和可用的左下角安全区域。新角色资产尚未通过验收时，不默认添加另一只临时几何小人充当成品；缺失模型时只允许使用已批准的静态备用图。

**本提案合并后只会增加设计说明和复现材料，不会自动改变应用显示。真正的实施 PR 必须另外提交业务代码、角色资产和验收证据。**
